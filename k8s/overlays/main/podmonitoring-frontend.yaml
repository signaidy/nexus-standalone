apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: nexus-frontend
  namespace: main
spec:
  selector:
    matchLabels:
      app: nexus
      tier: frontend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s