apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: nexus-backend
  namespace: uat
spec:
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: http
      path: /nexus/actuator/prometheus
      interval: 30s
