# Which environment to use for the backend env file:
# picks backend/.env.dev | .env.uat | .env.prod
APP_ENV=dev

# Host ports you’ll use to access the apps in your browser
BACKEND_PORT=8080
FRONTEND_PORT=5173

# The port your frontend container actually serves on (from its Dockerfile)
# Common values: 3000 (Node), 80 (Nginx), 4173/5173 (Vite preview/dev)
FRONTEND_CONTAINER_PORT=4173

# Frontend build/runtime base URL to reach the backend inside the Compose network
# NOTE: inside the Docker network, use the service name "backend" (not localhost)
VITE_API_BASE_URL=http://backend:8080